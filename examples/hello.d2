# Minimal D2 fixture used by verify-environment.ps1's d2w smoke test.
# Kept inside win-dot so the check is self-contained and can't rot
# when personal note repos move or get renamed.
hello -> world
