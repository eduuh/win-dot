#============================================================================
# TMUX CONFIGURATION (Windows / psmux)
# Tracked via win-dot.  Ported from WSL ~/.tmux.conf to maintain feature
# parity.  Items still WSL-only are marked TODO(parity).
#============================================================================

#----------------------------------------------------------------------------
# Core Settings
#----------------------------------------------------------------------------

# Terminal & colors
set -g default-terminal "tmux-256color"
set -as terminal-overrides ",*:RGB"

# Shell — PowerShell 7 on Windows (psmux launches pwsh by default, made
# explicit so the config is portable across machines).
set -g default-shell pwsh

# History
set -g history-limit 100000

# Timing
set -s escape-time 0
set -g display-panes-time 2000

# Mouse
set -g mouse on

# Window/pane numbering (start at 1)
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on

# Disable visual noise
set -g visual-activity off
set -g visual-bell off

#----------------------------------------------------------------------------
# Prefix & Core Bindings
#----------------------------------------------------------------------------

# Prefix: C-Space (matches WSL)
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Reload config
bind R source-file ~/.tmux.conf \; display "Config reloaded!"

# Clean up bindings that were removed (so reload re-applies cleanly)
unbind S
unbind J
unbind c
unbind C
unbind C-n
unbind C-s
unbind W
unbind N
unbind y
unbind k
unbind a
unbind `
unbind i
unbind C-r
unbind C-w

#----------------------------------------------------------------------------
# Pane Management
#----------------------------------------------------------------------------

# Splits (v=right, s=below) - keeps current path
bind v split-window -h -c "#{pane_current_path}"
bind s split-window -v -c "#{pane_current_path}"

# Pane navigation (arrow keys)
bind -r Left select-pane -L
bind -r Down select-pane -D
bind -r Up select-pane -U
bind -r Right select-pane -R

# Pane resizing (shift + arrow)
bind -r S-Left resize-pane -L 5
bind -r S-Down resize-pane -D 5
bind -r S-Up resize-pane -U 5
bind -r S-Right resize-pane -R 5

# Kill pane (with confirm)
bind q confirm-before -p "Kill pane? (y/n)" kill-pane

# Toggle zoom
bind z resize-pane -Z

#----------------------------------------------------------------------------
# Window Management
#----------------------------------------------------------------------------

# New window (keeps path)
bind w new-window -c "#{pane_current_path}"

# Rename window
bind r command-prompt -p "Rename:" "rename-window '%%'"

# Direct window access
bind 1 select-window -t 1
bind 2 select-window -t 2
bind 3 select-window -t 3
bind 4 select-window -t 4
bind 5 select-window -t 5

# Previous/next window
bind -r [ previous-window
bind -r ] next-window

# Cycle to next window (no prefix)
bind -n C-BSpace next-window

# Move windows left/right
bind -r < swap-window -t -1 \; previous-window
bind -r > swap-window -t +1 \; next-window

#----------------------------------------------------------------------------
# Session Management
#----------------------------------------------------------------------------

# Kill session and switch to next
bind t run-shell '(psmux switch-client -l || psmux switch-client -n) && psmux kill-session -t "#S" || psmux kill-session'

# Rename current session
bind . command-prompt -p "Rename session:" "rename-session '%%'"

# NOTE: psmux 3.3.4 floating pickers (choose-tree -Z*) aren't well-supported
# yet — `-Z` is silently dropped and choose-tree is normalized to
# choose-window. Re-enable once psmux matures.
# bind T choose-tree -Zs
# bind C-s choose-session -Z
# bind C-w choose-tree -Zw

# TODO(parity): Project picker — WSL binds C-o to a `tat` fzf popup. Needs a
# PowerShell port of ~/.bin/tmux/tat for Windows.
# bind C-o display-popup -E -w 80% -h 80% "tat.ps1"

#----------------------------------------------------------------------------
# Worktree Workflow (windows = worktrees) — TODO(parity)
#----------------------------------------------------------------------------
# These bindings depend on bash helpers in ~/.bin/tmux/. Need PowerShell
# ports before re-enabling on Windows.
#
# bind o display-popup -E -w 60% -h 50% "tmux-wt.ps1"
# bind W display-popup -E -w 60% -h 50% "tmux-wt.ps1 -New"
# bind X display-popup -E -w 60% -h 40% "tmux-wt-kill.ps1"
# bind D display-popup -E -w 60% -h 50% "tmux-wt-kill.ps1 -Pick"
# bind n display-popup -E -w 97% -h 97% -d "#{pane_current_path}" "tmux-note.ps1"

#----------------------------------------------------------------------------
# Copy Mode (vi-style)
#----------------------------------------------------------------------------

setw -g mode-keys vi

# Enter copy mode
bind Space copy-mode

# Selection
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi C-v send -X rectangle-toggle
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# Navigation
bind -T copy-mode-vi C-u send -X halfpage-up
bind -T copy-mode-vi C-d send -X halfpage-down
bind -T copy-mode-vi g send -X history-top
bind -T copy-mode-vi G send -X history-bottom

# Search
bind -T copy-mode-vi / command-prompt -p "/" "send -X search-forward '%%'"
bind -T copy-mode-vi ? command-prompt -p "?" "send -X search-backward '%%'"
bind -T copy-mode-vi n send -X search-again
bind -T copy-mode-vi N send -X search-reverse

# System clipboard — Windows uses clip.exe
bind -T copy-mode-vi y send -X copy-pipe-and-cancel "clip.exe"

# Paste
bind p paste-buffer
bind P choose-buffer

#----------------------------------------------------------------------------
# Status Line
#----------------------------------------------------------------------------

set -g status-interval 5
set -g status-position bottom
set -g status-justify left
set -g status-style "bg=default,fg=white"

# Left & right empty
set -g status-left ""
set -g status-right ""

# Top bar: session + branch
set -g status 2
set -g status-format[1] "#[align=right,bg=default]#[fg=colour243]#S #[fg=colour238]│ #[fg=cyan,bold]#(cd '#{pane_current_path}'; git branch --show-current 2>$null) "

# Window format
setw -g window-status-format "#[fg=colour243] #I #W "
setw -g window-status-current-format "#[fg=white,bold] #I #W "
setw -g window-status-separator "#[fg=colour238]│"

# Pane borders
set -g pane-border-style "fg=colour238"
set -g pane-active-border-style "fg=cyan"

# Messages
set -g message-style "bg=default,fg=yellow"

#----------------------------------------------------------------------------
# Popups & Tools
#----------------------------------------------------------------------------

# Lazygit
bind g display-popup -E -w 90% -h 90% -d "#{pane_current_path}" "lazygit"

# TODO(parity): nvim toggle (bind e) — depends on ~/.bin/tmux/tmux-edit.sh

#----------------------------------------------------------------------------
# Plugins — TODO(parity)
#----------------------------------------------------------------------------
# WSL uses TPM with tmux-resurrect and tmux-continuum. psmux ships its own
# plugin manager (PPM). Re-evaluate once we settle on a Windows plugin story.
#
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'tmux-plugins/tmux-continuum'
# set -g @resurrect-capture-pane-contents 'on'
# set -g @continuum-restore 'off'
# set -g @continuum-save-interval '0'
# run-shell '~/.tmux/plugins/tpm/tpm'
