# Private / per-machine overrides — never commit.
.gitconfig.local
.config/powershell/profile.local.ps1

# Tool scratch directories
.claude/

# Helix runtime is bundled with the binary; never track per-machine copies.
AppData/Roaming/helix/runtime/

# OS / editor cruft
$null
*.bak
*.swp
